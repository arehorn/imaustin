---
import type { HeroData } from "../types/emdash";

interface Props {
  hero: HeroData;
}

const { hero } = Astro.props;
---

<section id="hero" class="relative pt-40 pb-24 overflow-hidden">
  <div class="max-w-7xl mx-auto px-6">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
      {/* Text column */}
      <div class="lg:col-span-7 space-y-10">
        <div class="space-y-6">
          <span class="inline-block px-4 py-1 rounded-full border border-[#C9A86C]/30 bg-[#C9A86C]/10 text-[#FF5A1F] text-xs uppercase tracking-[0.3em] font-bold animate-fade-in-up">
            {hero.badge_text}
          </span>

          <h1 class="text-5xl sm:text-6xl lg:text-7xl xl:text-8xl font-extrabold font-display leading-[1.05] tracking-tighter text-white animate-fade-in-up animation-delay-100">
            <span class="bg-clip-text text-transparent bg-gradient-to-r from-[#FF5A1F] to-[#C9A86C]">{hero.headline}</span>
          </h1>
        </div>

        <p class="text-lg sm:text-xl text-gray-400 max-w-2xl leading-relaxed animate-fade-in-up animation-delay-200">
          {hero.subheading}
        </p>

        <div class="flex flex-wrap gap-4 pt-2 animate-fade-in-up animation-delay-300">
          <a
            href="#connect"
            class="px-8 py-4 rounded-2xl bg-gradient-to-r from-[#C9A86C] to-[#FF5A1F] text-white font-black tracking-wide hover:brightness-110 transition-all transform hover:-translate-y-1 shadow-[0_0_30px_rgba(201,168,108,0.4)]"
          >
            Let&apos;s Connect
          </a>
          {hero.resume_url && (
            <a
              href={hero.resume_url}
              download
              class="px-8 py-4 rounded-2xl glass-card text-white font-bold tracking-wide hover:bg-white/10 transition-all inline-flex items-center gap-3 group"
            >
              View Resume
              <span class="material-symbols-outlined group-hover:translate-y-1 transition-transform text-[#FF5A1F]">arrow_downward</span>
            </a>
          )}
        </div>

        <p class="text-sm text-gray-500 uppercase tracking-[0.3em] animate-fade-in-up animation-delay-400">
          {hero.cta_text}
        </p>
      </div>

      {/* Headshot */}
      <div class="lg:col-span-5 flex justify-center lg:justify-end">
        <div class="relative w-full aspect-[4/5] max-w-md p-2 rounded-[2rem] glass-card rotate-3 hover:rotate-0 transition-transform duration-700 shadow-[0_0_50px_rgba(201,168,108,0.2)] group animate-float">
          <div class="absolute -inset-4 bg-gradient-to-tr from-[#C9A86C] to-[#FF5A1F] opacity-20 blur-3xl group-hover:opacity-40 transition-opacity" aria-hidden="true"></div>
          <img
            src={hero.headshot_url}
            alt={hero.headshot_alt || "Austin Rehorn"}
            width="480"
            height="600"
            class="relative w-full h-full object-cover object-top rounded-[1.8rem] brightness-90 contrast-110"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>
</section>
