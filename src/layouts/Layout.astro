---
import "../styles/globals.css";
import Nav from "../components/Nav";

interface Props {
  title?: string;
  description?: string;
}
const {
  title = "Austin Rehorn — Enterprise Account Management & AI Automation",
  description = "Trust and Relationships by choice, AI automation by necessity. Enterprise account manager and AI builder based in Mt. Pleasant, SC.",
} = Astro.props;
---
<!doctype html>
<html lang="en" class="h-full antialiased">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@500;600;700;800&family=DM+Sans:wght@400;500;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="min-h-full flex flex-col bg-[#E0E5EC]">
    <Nav client:load />
    <slot />
    <footer class="py-10">
      <div class="max-w-6xl mx-auto px-6 flex items-center justify-between">
        <p class="text-sm text-[#6B7280]">© {new Date().getFullYear()} Austin Rehorn</p>
        <div
          class="w-8 h-1 rounded-full"
          style="background: linear-gradient(90deg, #6C63FF, #38B2AC)"
        />
      </div>
    </footer>
  </body>
</html>
